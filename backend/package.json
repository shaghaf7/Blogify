{
  "name": "backend",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "nodemon index.js",
    "build": "rm -rf dist && mkdir dist && cp -r index.js routes controllers middlewares utils package.json package-lock.json certs dist/ && cd dist && npm install --production && cd .. && zip -r backend.zip dist"
  },
  "keywords": [
    "sql",
    "backend"
  ],
  "author": "shaghaf",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "@google/generative-ai": "^0.24.1",
    "bcryptjs": "^3.0.2",
    "body-parser": "^2.2.0",
    "cors": "^2.8.5",
    "dotenv": "^17.2.1",
    "expres": "^0.0.5",
    "express": "^5.1.0",
    "jsonwebtoken": "^9.0.2",
    "mysql2": "^3.14.3",
    "nodemon": "^3.1.10",
    "serverless-http": "^3.2.0"
  }
}
